# Navigation
alias ll = ls -la
alias .. = cd ..
alias ... = cd ../..

# Git
alias g = git
alias gst = git status
alias ga = git add
alias gc = git commit
alias gp = git push
alias gl = git log --oneline --graph --decorate
alias gf = git fetch
alias gpl = git pull

# Editor
alias v = nvim
alias vim = nvim

# tmux (future-proof)
alias t = tmux
alias ta = tmux attach
alias tn = tmux new
